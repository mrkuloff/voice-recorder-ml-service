# Сервис для транскрибации аудио файлов

Проект Умный диктофон

Сервис работает в связке с очередью RabbitMQ.

Работает только с аудио в формате .m4a . Проблем с использованием других форматов нет, в проекте используется этот.

Алгоритм запуска:

1. Скачать проект
``` bash
git clone
```
2. Перейти в проект
``` bash
cd voice-recorder-ml-service
```
3. Собрать Docker image
``` bash
docker build -t voice-recorder-ml-service . 
```
4. Образ создан. Запускаем общий docker-compose файл с бекендом проекта. Обязательно в docker-compose должен быть RabbitMQ

TODO:
Для production версии в ```data.config.py``` вынести логику использования переменных извне.
Возможно лучше вынести инициализацию модели при старте приложения. Блокер: при ```device="cuda``` могут быть коллизии

Протестировать приложение можно:
* Нужно иметь готовый Docker image
* Запустить ``` docker-compose -f test.docker-compose.yml up -d```
* Загрузить аудио в формате .m4a в minio бакет
* Запустить файл ```test.main.py``` с корректным номером записи
* В конце должна быть ошибка связи с сервисом бизнес логики. Это нормально.
